import { test } from '@playwright/test';

/**
 * Playwright Test Agents seed file (Generator bootstrap).
 * Skipped in CI — use tests/e2e/login.spec.ts as the live reference suite.
 */
test.skip('agent seed placeholder', async () => {});
